# MERIDIAN Pathfinder

CLI-first ML decision engineering framework with gated mode execution and artifact provenance.

## Development

Install dependencies and run the CLI:

```bash
uv sync
uv run meridian --help
```

Environment:
- `ANTHROPIC_API_KEY`: used by Anthropic LLM provider (do not hardcode keys).

